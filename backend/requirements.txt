# Основные зависимости для FastAPI (веб-фреймворк)
fastapi==0.95.0        # Сам FastAPI фреймворк для создания API
                       # Фиксируем версию для стабильности (0.95.0)

# ASGI-сервер для запуска FastAPI приложения
uvicorn==0.21.1        # Легковесный ASGI-сервер
                       # Версия совместима с FastAPI 0.95.0

# Основные ML-зависимости
torch==1.13.1          # PyTorch для работы нейронных сетей
                       # Версия с поддержкой CPU/GPU операций
                       # Важно для работы предобученной модели ECGCNN

# Библиотеки для работы с данными
numpy==1.24.2          # Основная библиотека для численных операций
                       # Требуется для обработки ЭКГ-сигналов
pandas>=1.3.0          # Для работы с табличными данными (CSV)
                       # Минимальная версия 1.3.0, но можно новее
scipy==1.10.1          # Научные вычисления (используется stats.zscore)
                       # Версия совместима с numpy 1.24.2

# Специальные зависимости для обработки сигналов
PyWavelets==1.4.1      # Вейвлет-преобразования для денойзинга ЭКГ
                       # Критично для предобработки сигналов

# Дополнительные утилиты
python-multipart==0.0.6 # Для обработки multipart/form-data (загрузка файлов)
                       # Требуется FastAPI для работы с UploadFile