# Основные зависимости для Streamlit-приложения

# Ядро Streamlit - минимальная версия 1.18.1 (фиксируем для стабильности)
streamlit==1.18.1       # Веб-фреймворк для создания интерактивных дашбордов
                        # Версия включает все основные компоненты и исправления багов

# Для HTTP-запросов к бэкенду (REST API)
requests==2.28.2        # Простая и удобная библиотека для HTTP-запросов
                        # Версия с исправлениями уязвимостей и стабильным API

# Основные научные вычисления
numpy==1.24.2           # Фундаментальная библиотека для численных операций
                        # Требуется для обработки массивов данных ЭКГ
                        # Совместимость с другими научными пакетами

# Для работы с табличными данными (CSV, DataFrame)
pandas>=1.3.0           # Библиотека для анализа и обработки табличных данных
                        # Минимальная версия 1.3.0 (можно использовать более новые)
                        # Не фиксируем строго для совместимости с другими пакетами

# Визуализация данных (основные графики)
matplotlib==3.7.1       # Основная библиотека для построения графиков
                        # Используется для отрисовки временных рядов ЭКГ
                        # Стабильная версия с поддержкой всех нужных функций

# Интерактивная визуализация (альтернатива matplotlib)
altair==4.2.0           # Библиотека для декларативной визуализации
                        # Хорошо интегрируется со Streamlit
                        # Позволяет создавать интерактивные графики